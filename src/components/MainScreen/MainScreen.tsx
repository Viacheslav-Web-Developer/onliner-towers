import React from 'react';
import Header from "@/components/Header/Header";

const MainScreen = () => {
    return (
        <div>
            <Header/>
        </div>
    );
};

export default MainScreen;